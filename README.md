**To run this project locally make sure docker and docker-compose is installed in your machine**
After insuring the installation run these command in project directory

`docker-compose build`

`docker-compose up`